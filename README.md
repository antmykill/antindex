# AntIndex

这是一个基于静态 HTML 的个人网站项目。

## 发布到 GitHub Pages

1. 在 GitHub 创建一个新的仓库。
2. 把这个目录上传到仓库。
3. 进入仓库设置 -> Pages。
4. 选择部署来源为“Deploy from a branch”，分支选择 `main`，根目录为 `/`。
5. 等待几分钟后访问你的 GitHub Pages 地址。
